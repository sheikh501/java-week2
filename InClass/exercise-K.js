const std = ["Sheikh","jarif","juha",]
function grtng (name){
    
    console.log (`hello ${name} good to see you`);
}
// console.logg(grtng(std[0]));
// console.logg(grtng(std[1]));
// console.logg(grtng(std[2]));
for (i = 0; i<std.length; i++){
grtng(std[i]);
}